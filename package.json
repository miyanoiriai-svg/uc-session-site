{
  "name": "uc-session-site",
  "version": "1.0.0",
  "private": true,
  "description": "UC Session brand site + column (SEO magazine) + Decap CMS",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}
